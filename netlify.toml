# ==============================================================================
# 檔案路徑: netlify.toml
# 版本: v31.7 - MPA/SPA 混合路由最終版
# ==============================================================================

[build]
  command = "echo '無需建置指令'"
  publish = "test-shiporder"
  functions = "test-shiporder/netlify/functions"
  
  [build.environment]
    NODE_VERSION = "18"

# 所有路由規則在 test-shiporder/_redirects
# 此處不重複設定 [[redirects]]

[dev]
  framework = "#static"
  publish = "."

[context.production.environment]
  SUPABASE_URL = "https://your-project.supabase.co"
  SUPABASE_ANON_KEY = "your-anon-key"